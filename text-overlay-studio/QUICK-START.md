# 🚀 Quick Start Guide - ทดสอบทันที

## ⚡ วิธีที่ 1: ทดสอบบนเครื่องตัวเอง (ไม่ต้องตั้งค่าอะไร)

### **ขั้นตอน:**

#### **1. เปิด Terminal/Command Prompt**

**Windows:**
- กด `Win + R` → พิมพ์ `cmd` → Enter

**Mac/Linux:**
- กด `Cmd + Space` → พิมพ์ `Terminal` → Enter

#### **2. ไปที่โฟลเดอร์ที่โคลนมา**

```bash
cd path/to/check-all-workflow/text-overlay-studio
```

#### **3. เริ่ม Web Server**

**Python 3 (แนะนำ):**
```bash
python -m http.server 8000
```

**Python 2:**
```bash
python -m SimpleHTTPServer 8000
```

**Node.js (ถ้ามี):**
```bash
npx http-server -p 8000
```

**PHP (ถ้ามี):**
```bash
php -S localhost:8000
```

#### **4. เปิดเบราว์เซอร์**

```
http://localhost:8000
```

หรือ

```
http://127.0.0.1:8000
```

---

## 📱 วิธีที่ 2: ทดสอบบนมือถือ (Same Wi-Fi)

### **ขั้นตอน:**

#### **1. หา IP Address ของคอมพิวเตอร์**

**Windows:**
```bash
ipconfig
```
หา `IPv4 Address` เช่น `192.168.1.100`

**Mac:**
```bash
ifconfig | grep "inet "
```

**Linux:**
```bash
hostname -I
```

#### **2. เริ่ม Server (เหมือนวิธีที่ 1)**
```bash
cd text-overlay-studio
python -m http.server 8000
```

#### **3. เปิดมือถือ**
- ต้อง**เชื่อมต่อ Wi-Fi เดียวกัน**กับคอมพิวเตอร์
- เปิดเบราว์เซอร์บนมือถือ
- พิมพ์: `http://192.168.1.100:8000` (ใช้ IP ที่หาได้)

---

## ☁️ วิธีที่ 3: ทดสอบแบบเต็มรูปแบบ (กับ Cloudinary)

### **ต้องตั้งค่าก่อน:**

#### **1. สมัคร Cloudinary (ฟรี)**
1. ไปที่ [https://cloudinary.com/users/register_free](https://cloudinary.com/users/register_free)
2. กรอกข้อมูลและยืนยันอีเมล
3. จดบันทึก **Cloud Name** จาก Dashboard

#### **2. สร้าง Upload Preset**
1. ไปที่ [https://console.cloudinary.com/settings/upload](https://console.cloudinary.com/settings/upload)
2. กด **Add upload preset**
3. ตั้งค่า:
   - **Preset name**: `upload-image`
   - **Signing mode**: `Unsigned` ⚠️ สำคัญ!
   - **Folder**: `text-overlay` (optional)
4. กด **Save**

#### **3. แก้ไข config.js**

เปิดไฟล์ `text-overlay-studio/config.js`:

```javascript
const CONFIG = {
    cloudinary: {
        cloudName: 'YOUR_CLOUD_NAME', // ⬅️ ใส่ Cloud Name ที่คัดลอกมา
        uploadPreset: 'upload-image',
    },
    // ... ส่วนอื่นไม่ต้องแก้ถ้ายังไม่มี n8n
};
```

**หา Cloud Name:**
- ไปที่ [Dashboard](https://console.cloudinary.com/)
- มองหา **Product Environment Credentials**
- คัดลอก **Cloud name** (ตัวอย่าง: `demo`, `my-shop-123`)

#### **4. ทดสอบ Upload**
1. เปิด `http://localhost:8000`
2. เลือก **🖼️ เพิ่มข้อความลงรูปภาพ**
3. อัปโหลดรูป
4. พิมพ์ข้อความ
5. กด **🚀 สร้างรูปภาพ**

✅ ถ้าสำเร็จ จะเปิดแท็บใหม่แสดงรูปที่มี text overlay!

---

## 🧪 วิธีที่ 4: ทดสอบ n8n Webhook (ถ้ามี n8n)

### **ขั้นตอน:**

#### **1. สร้าง Workflow ใน n8n**

**Node 1: Webhook**
- **HTTP Method**: `POST`
- **Path**: `/text-overlay` (หรืออะไรก็ได้)
- คัดลอก **Webhook URL**

**Node 2: Google Sheet / Database** (ถ้าต้องการ)
- เชื่อมต่อกับ Google Sheet
- Map ข้อมูลที่ได้รับจาก Webhook

**Node 3: Set** (optional)
- แปลงข้อมูล หรือ format ตามต้องการ

#### **2. แก้ไข config.js**

```javascript
const CONFIG = {
    // ... cloudinary config ...
    n8n: {
        webhookURL: 'YOUR_N8N_WEBHOOK_URL', // ⬅️ ใส่ URL ที่คัดลอกมา
    },
};
```

#### **3. ทดสอบ**
1. เปิดแอพ → สร้างรูป text overlay
2. เช็คที่ n8n Workflow → ควรเห็นข้อมูลเข้ามา
3. ดูใน Google Sheet ว่ามีข้อมูลบันทึกหรือยัง

---

## 🎯 ทดสอบแบบไม่ต้องตั้งค่าอะไร (Demo Mode)

### **วิธีเร็วที่สุด:**

#### **1. เปิดไฟล์ HTML โดยตรง**
- ไปที่โฟลเดอร์ `text-overlay-studio/`
- ดับเบิลคลิก `index.html`
- จะเปิดในเบราว์เซอร์

⚠️ **ข้อจำกัด:**
- Upload ไม่ได้ (ต้องมี Cloudinary)
- ไม่สามารถส่งข้อมูลไป n8n ได้
- แต่ดู UI/UX ได้

#### **2. ใช้ CodePen / JSFiddle (ทดสอบ UI)**
- คัดลอกโค้ด HTML/CSS/JS
- วางใน [CodePen.io](https://codepen.io) หรือ [JSFiddle.net](https://jsfiddle.net)
- รันทดสอบได้ทันที

---

## 🐛 Troubleshooting

### **ปัญหา: Server ไม่ขึ้น**

```bash
# ลองใช้ port อื่น
python -m http.server 8080

# แล้วเปิด http://localhost:8080
```

### **ปัญหา: มือถือเข้าไม่ได้**

1. ✅ เช็ค Wi-Fi เดียวกันหรือไม่
2. ✅ ปิด Firewall ชั่วคราว
3. ✅ ลอง IP อื่นๆ จาก `ipconfig`

### **ปัญหา: Upload ไม่ได้**

1. ✅ เช็ค `cloudName` ถูกต้องหรือไม่
2. ✅ เช็ค Upload Preset เป็น **Unsigned** หรือไม่
3. ✅ เปิด Console (F12) ดู error message

### **ปัญหา: n8n ไม่ได้รับข้อมูล**

1. ✅ เช็ค Webhook URL ถูกต้องหรือไม่
2. ✅ เช็ค n8n Workflow **เปิดอยู่** หรือไม่
3. ✅ ลอง Test ด้วย Postman/Insomnia ก่อน

---

## 📸 ตัวอย่างการทดสอบ

### **Test Case 1: ป้ายลดราคา**
1. อัปโหลดรูปสินค้า
2. พิมพ์: `ลด 50%`
3. เลือก **🏷️ ป้ายลดราคา**
4. เลือกตำแหน่ง **มุมบนขวา**
5. เปิด **ข้อความโค้ง** ✅
6. กด **🚀 สร้างรูปภาพ**

**ผลลัพธ์ที่คาดหวัง:**
- รูปภาพมีข้อความ "ลด 50%" สีขาว พื้นหลังแดง
- ข้อความโค้งเป็นส่วนโค้ง
- อยู่มุมบนขวา

### **Test Case 2: ลายน้ำ**
1. อัปโหลดรูป
2. พิมพ์: `@myshop`
3. เลือก **🔖 ลายน้ำ**
4. เลือก **มุมล่างขวา**
5. กด **🚀 สร้างรูปภาพ**

**ผลลัพธ์:**
- ข้อความเล็กๆ มุมล่างขวา
- สีดำ โปร่งแสง 60%

### **Test Case 3: วิดีโอ Promo**
1. อัปโหลดวิดีโอ
2. พิมพ์: `New Arrival`
3. ตั้งเวลา: เริ่ม `2` จบ `10` วินาที
4. เลือก **📌 หัวข้อ**
5. เลือก **บนกลาง**
6. กด **🚀 สร้างวิดีโอ**

**ผลลัพธ์:**
- วิดีโอมีข้อความปรากฏวินาทีที่ 2-10 เท่านั้น
- อยู่ตรงกลางด้านบน

---

## 💡 Tips สำหรับการทดสอบ

### **เทสบนมือถือจริง:**
- ใช้ Chrome DevTools → F12 → Toggle Device Toolbar (Ctrl+Shift+M)
- เลือกอุปกรณ์: iPhone 12, Samsung Galaxy S21
- ทดสอบ touch events, responsive design

### **เทส Performance:**
- เปิด Console (F12) → Network Tab
- ดูว่า upload ใช้เวลานานไหม
- ดูว่ามี error หรือไม่

### **เทส Curved Text:**
- ลองมุมต่างๆ:
  - 180° = โค้งครึ่งวงกลม
  - 360° = วงกลมเต็ม
  - -180° = โค้งกลับด้าน

### **เทส Fonts:**
- ลอง Thai fonts: Mitr, Kanit, Prompt
- ลอง English fonts: Roboto, Montserrat
- ดูว่า render สวยไหม

---

## 🎓 ขั้นตอนแนะนำ (เรียงลำดับ)

1. ✅ **ทดสอบ Local ก่อน** (วิธีที่ 1)
   - ดู UI/UX ว่าชอบไหม
   - ทดสอบ responsive design

2. ✅ **ทดสอบบนมือถือ** (วิธีที่ 2)
   - ดู touch controls
   - ทดสอบ upload รูปจากกล้อง

3. ✅ **Setup Cloudinary** (วิธีที่ 3)
   - ทดสอบ upload จริง
   - ทดสอบ curved text

4. ✅ **Setup n8n** (วิธีที่ 4)
   - เชื่อมต่อ Google Sheet
   - ทดสอบ workflow ทั้งระบบ

5. ✅ **Deploy จริง**
   - Upload ไป Hostinger หรือ hosting
   - ให้คนอื่นทดสอบ

---

## 🚀 พร้อมเริ่มทดสอบแล้ว!

เริ่มจาก**วิธีที่ 1**ได้เลยครับ ไม่ต้องตั้งค่าอะไร แค่เปิด terminal และรันคำสั่ง:

```bash
cd text-overlay-studio
python -m http.server 8000
```

แล้วเปิด: **http://localhost:8000**

---

มีปัญหาตรงไหนบอกได้เลยครับ! 😊
